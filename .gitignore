node_modules/
*.vsix
.DS_Store

# Generated by the manager's Copilot routing panel. Both pin picker ids and
# qualified model names that only exist in one user's settings, so they are
# per-machine output, not source.
.github/agents/poly-subagent.agent.md
.github/copilot-instructions.md
